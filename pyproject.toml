[build-system]
requires = ["setuptools>=69"]
build-backend = "setuptools.build_meta"

[project]
name = "gaussian-splat-modal"
version = "0.1.0"
description = "Minimal Modal runner for COLMAP and Nerfstudio Splatfacto"
requires-python = ">=3.10"
dependencies = ["modal>=1.5,<2"]
